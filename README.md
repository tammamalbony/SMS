## About SMS

- composer 
``` install
    composer install
    composer update


    composer dump-autoload
```

### config
```
php artisan vendor:publish --tag=translatable
```

- php artisan 

``` initail
    php artisan migrate

```

- php seeders

``` seeders
    php artisan db:seed
    
```